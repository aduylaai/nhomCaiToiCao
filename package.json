{
  "dependencies": {
    "@tinacms/cli": "^1.6.2",
    "next": "^14.2.8",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tinacms": "^2.2.2"
  },
  "scripts": {
    "dev": "tinacms dev -c \"next dev\"",
    "build": "tinacms build && next build"
  }
}
